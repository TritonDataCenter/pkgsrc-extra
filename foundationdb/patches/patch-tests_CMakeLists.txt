$NetBSD$

Do not register the token-authorization tests: their venv setup
pip-installs from the network at test time.

--- tests/CMakeLists.txt.orig	2026-07-12 00:00:00.000000000 +0000
+++ tests/CMakeLists.txt
@@ -543,7 +543,7 @@
     endif()
   endif()

-  if(NOT OPEN_FOR_IDE AND NOT WIN32 AND NOT USE_SANITIZER)
+  if(FALSE)
     # setup venv for testing token-based authorization
     if(APPLE)
       set(ld_env_name "DYLD_LIBRARY_PATH")
